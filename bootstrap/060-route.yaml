apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: compliance
  namespace: compliance-chatbot
spec:
  to:
    kind: Service
    name: compliance
  port:
    targetPort: 80
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect