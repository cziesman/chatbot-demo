kind: BuildConfig
apiVersion: build.openshift.io/v1
metadata:
  name: compliance
  namespace: compliance-chatbot
spec:
  nodeSelector: null
  output:
    to:
      kind: ImageStreamTag
      name: 'compliance:latest'
  resources: {}
  successfulBuildsHistoryLimit: 5
  failedBuildsHistoryLimit: 5
  strategy:
    type: Docker
    dockerStrategy: {}
  postCommit: {}
  source:
    type: Git
    git:
      uri: 'https://github.com/cziesman/chatbot-demo.git'
      ref: main
  triggers:
    - type: ConfigChange
  runPolicy: Serial

