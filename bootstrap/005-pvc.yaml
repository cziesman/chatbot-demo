kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: compliance
  namespace: compliance-chatbot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: gp3-csi
  volumeMode: Filesystem
